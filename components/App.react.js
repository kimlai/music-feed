var React = require('react');
var Feed = require('./Feed.react');

module.exports = React.createClass({
    render: function () {
        return (
            <div>
                <Feed />
            </div>
        );
    }
});
